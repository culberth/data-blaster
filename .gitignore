# Maven build output
target/

# jpackage app-images. Deliberately outside target/ so `mvn clean` on one variant
# does not delete the other variant's already-built image — see scripts/.
dist/

# IDEs
.idea/
*.iml
.vscode/
.settings/
.classpath
.project
nbproject/private/

# OS
Thumbs.db
Desktop.ini
.DS_Store
