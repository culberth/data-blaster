spring.main.web-application-type=servlet
spring.main.banner-mode=off
server.port=8080
# Bind the companion HTTP layer to loopback only: it has no authentication, so a wildcard bind
# would expose every endpoint under /api to anyone on the same network.
server.address=127.0.0.1
